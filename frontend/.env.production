# Production environment — used when running `npm run build`
# Use RELATIVE path so nginx can proxy /api/* to backend
# Do NOT use http://localhost:8000 here — it breaks in production
VITE_API_BASE_URL=/api
